spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: ""

  flyway:
    enabled: false

  stripe:
  webhook-secret: test

  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  secret: your-super-long-random-secret-at-least-32-bytes

app:
  admin:
    email: ${APP_ADMIN_EMAIL:admin@test.com}
    password: ${APP_ADMIN_PASSWORD:testpassword}
  frontend-base-url: http://localhost:3000
security:
  booking-token:
    pepper: test-pepper

resend:
  api-key: test-key