spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: ""

  flyway:
    enabled: false

  stripe:
  webhook-secret: test

  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  secret: your-super-long-random-secret-at-least-32-bytes

security:
  booking-token:
    pepper: test-pepper

app:
  frontend-base-url: http://localhost:3000

resend:
  api-key: test-key